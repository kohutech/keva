import React from 'react';

import { classNames } from '../../util/util'
import { StateProp } from '../../state/state'

import './DoubleEntry.css'

export default function DoubleEntry({ state }: StateProp) {
    return (
        <div>
            <h1> Nothing here yet :/</h1>
        </div>
  );
}
